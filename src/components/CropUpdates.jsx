import React from "react";
import styles from "../components/CropUpdates.module.css";

const CropUpdates = () => {
  return <main className={styles.mainContainer}>Crop Update</main>;
};

export default CropUpdates;
